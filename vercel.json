{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "corepack enable && yarn install --immutable",
  "buildCommand": "yarn build && yarn workspace react-native-waveforms-docs build",
  "outputDirectory": "docs/out"
}
